import React from 'react';
import { Text, View } from 'react-native';

interface IProps {}

function CircularScheduleTable({}: IProps) {
	return (
		<View style={{}}>
			<Text>CircularScheduleTable</Text>
		</View>
	);
}

export default CircularScheduleTable;
